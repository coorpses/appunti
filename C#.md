✨ ​Program.cs
---
Da dove inizia il codice

✨ List&lt;T&gt;
---
```csharp
var names = new List<string> { "scott", "ana" };

Console.WriteLine(names[^1]) // count from the back, last element

foreach (var name in names[2..4]) {} // range from 2nd to 4th
```

✨ OOP (object-oriented programming)
---
è un paradigma fondamentale che organizza il codice in "oggetti" dotati di dati (proprietà) e comportamenti (metodi).
4 pilastri dell'OOP: Incapsulamento, Ereditarietà, Polimorfismo e Astrazione
